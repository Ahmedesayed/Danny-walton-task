import { CountPrefixPipe } from './count-prefix.pipe';

describe('CountPrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new CountPrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
